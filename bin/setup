#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Init and update git submodules
git submodule init
git submodule update

# Vendor KaTeX js
cp vendor/KaTeX/dist/katex.js lib/js/katex.js
